#include "subserver.h"
#include "client.h"
#include "item.h"
#include <vector>

using namespace std;

private 
class Server
{
private:
    vector<Subserver> subservers;
    int period;
public:
 Server(/* args */);
     Server();
};
 Server: Server(/* args */)
{
}
 Server:: Server()
{
}
