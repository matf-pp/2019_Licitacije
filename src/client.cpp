#include<string>
#include<vector>
#include "subserver.h"

private class Client{
private:
    double money;
    string username,password;
    std::vector<Subserver> subservers;
public:
    Client() { }
    ~Client(){}
    Client
}